apiVersion: v1
kind: Service
metadata:
  name: {{ include "arepos-server.fullname" . }}
  labels:
    {{- include "arepos-server.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "arepos-server.selectorLabels" . | nindent 4 }}
    {{- if .Values.blueGreen.enabled }}
    app.kubernetes.io/color: {{ .Values.blueGreen.activeColor | default "blue" | quote }}
    {{- end }}
